{
  "description": "Display a rewritten prompt and require approval before acting on every task request.",
  "hooks": {
    "UserPromptSubmit": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "echo 'MANDATORY: Prompt-only: output `You Suck At Prompting Rewritten prompt:` plus a fenced rewrite; no gate or execution. Otherwise, do not execute first. Output that heading and fenced rewrite, then `Reply with an acknowledgement to use this prompt.` Missing info: output `Draft rewritten prompt:` plus `[NEEDED: ...]`, ask one question, no acknowledgement. A clear affirmative acknowledgement regardless of capitalization executes latest rewrite once. Edits or qualifications reset. Preserve safety and authority.'",
            "commandWindows": "powershell.exe -NoProfile -NonInteractive -Command \"[Console]::Out.WriteLine('MANDATORY: Prompt-only: output `You Suck At Prompting Rewritten prompt:` plus a fenced rewrite; no gate or execution. Otherwise, do not execute first. Output that heading and fenced rewrite, then `Reply with an acknowledgement to use this prompt.` Missing info: output `Draft rewritten prompt:` plus `[NEEDED: ...]`, ask one question, no acknowledgement. A clear affirmative acknowledgement regardless of capitalization executes latest rewrite once. Edits or qualifications reset. Preserve safety and authority.')\"",
            "timeout": 5,
            "additionalContextLimit": 512
          }
        ]
      }
    ]
  }
}
